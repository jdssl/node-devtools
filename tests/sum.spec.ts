describe('Sum', () => {
	it('should return a correct sum value', () => {
		const test = 'Test testadad'
		const as = 'test dsssssd'

		expect(1 + 1).toBe(2)
		expect(2 + 2).toBe(4)
	})
	it('test', () => {
		const messa = "test"
		debugger
	})
})
