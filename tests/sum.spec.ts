describe("Sum", () => {
    it("should return a correct sum value", () => {
        expect(1 + 1).toBe(2);
        expect(2 + 2).toBe(4);
    });
    it("test", () => {
        const message = "test";

        expect(message).toEqual("test");
    });
});
